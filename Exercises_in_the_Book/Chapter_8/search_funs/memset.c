#include <stdio.h>
#include <string.h>

/*
memset copies the value of byte in its second argument into the first
n bytes of the object pointed to by its first argument
*/


int main(){
	char string1[15]="BBBBBBBBBBBBBB";
	printf("string1 = %s\n", string1);
}
