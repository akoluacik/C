#include<stdio.h>

int main(){
	
	int n,bin=0,digit=1,rem;
	printf("Enter a number:");scanf("%d", &n);
	while(n>0){
		rem = n % 2;
		bin = bin*10 + rem;
		n = n/2;
	}
	printf("%d", bin);
	return 0;
}